
//sum function. This will take an array of integers rather than as in the example where two integers were the parameters.
/*let findSum = array => {
    let sum = 0;
    for (let i = 0; i < array.length; i++) {
        sum += array[i];
    }
    return sum;
};
*/

//subtract function
//let subtractNumbers = (a, b) => b - a;

// Multiply Function
//let multiplyNumber = (c, d) => c * d;

// Divide Function
/*let divideNumbers = (firstNum, secondNum) => {
  if (secondNum == 0) {
    //if statement to check if the second number is not 0

    return "This is not possible";
  } else {
    return firstNum / secondNum;
  }
};*/

//to check if the divideNumbers function works
//console.log(divideNumbers(15, 3)); //will return 5
//console.log(divideNumbers(20, 0)); // will return message this is not possible

// creating an array
//const integers = [1, 2, 3];

// calling findsum function and logging the value
// console.log(findSum(integers)); // Output: 15

//calling the subtractNumbers function with first two arrray elements
//console.log(subtractNumbers (integers [0], integers [1]))

//calling the multiplyNumber function
//console.log(multiplyNumber (integers [2], integers [0]))

//calling divideNumbers function using the sum of findSum and dividing by third element in array
//console.log(divideNumbers (findSum(integers), integers [2]))